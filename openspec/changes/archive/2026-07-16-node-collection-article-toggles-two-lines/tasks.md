## 1. Layout UI

- [x] 1.1 Dans `CardLayoutSettings.tsx`, remplacer le conteneur une ligne (`flex justify-between`) des switchs Image / Title / Text / Label par une grille 2×2 (`grid grid-cols-2` ou équivalent)
- [x] 1.2 Placer Image + Title sur la première ligne, Text + Label sur la seconde, sans changer les handlers `content.show.*`

## 2. Vérification

- [x] 2.1 Vérifier visuellement dans le builder (NodeCollection article, settings Style / Card) que les quatre switchs tiennent sans débordement horizontal
