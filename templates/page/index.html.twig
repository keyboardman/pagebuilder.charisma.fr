{% extends 'base.html.twig' %}

{% block title %}Pages{% endblock %}

{% block body %}
<div class="space-y-6">
    <div class="flex items-center justify-between">
        <h1 class="text-2xl font-bold tracking-tight">Pages</h1>
        <div class="flex gap-2">
            <a href="{{ path('app_page_new') }}" class="btn btn-primary">Nouvelle page</a>
        </div>
    </div>

    {% include 'filemanager/_media_guidelines.html.twig' %}

    <div class="card overflow-hidden">
        <div class="overflow-x-auto">
            <table class="table-shadcn">
                <thead>
                    <tr>
                        <th>Titre</th>
                        <th>Slug</th>
                        <th>Lien</th>
                        <th>Thème</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for page in pages %}
                    <tr>
                        <td>{{ page.title }}</td>
                        <td><code class="text-xs">{{ page.slug }}</code></td>
                        <td>
                            <a href="{{ absolute_url(path('app_page_render', { idOrSlug: page.slug })) }}" target="_blank" rel="noopener">
                                {{ absolute_url(path('app_page_render', { idOrSlug: page.slug })) }}
                            </a>
                        </td>
                        <td>{{ page.theme.name }}</td>
                        <td class="text-end">
                            <div class="flex gap-2 justify-end items-center">
                                <a href="{{ path('app_page_render', { idOrSlug: page.slug }) }}" class="btn btn-ghost btn-sm" target="_blank" rel="noopener">Prévisualiser</a>
                                <a href="{{ path('app_page_builder', { id: page.id }) }}" class="btn btn-ghost btn-sm" target="_blank" rel="noopener">Builder</a>
                                <a href="{{ path('app_page_edit', { id: page.id }) }}" class="btn btn-ghost btn-sm">Modifier</a>
                                <form method="post" action="{{ path('app_page_duplicate', { id: page.id }) }}" class="inline">
                                    <input type="hidden" name="_token" value="{{ csrf_token('duplicate' ~ page.id) }}">
                                    <button type="submit" class="btn btn-ghost btn-sm">Dupliquer</button>
                                </form>
                                <form method="post" action="{{ path('app_page_delete', { id: page.id }) }}" class="inline" onsubmit="return confirm('Supprimer la page « {{ page.title|e('js') }} » ?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page.id) }}">
                                    <button type="submit" class="btn btn-ghost btn-sm text-destructive">Supprimer</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    {% else %}
                    <tr><td colspan="4" class="text-muted-foreground">Aucune page.</td></tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
