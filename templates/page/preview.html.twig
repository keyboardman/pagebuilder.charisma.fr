<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page.title }}</title>
    {% include '_builder_public_icon_mask_urls.html.twig' %}
    {% if page.description %}
        <meta name="description" content="{{ page.description }}">
    {% endif %}
    
    {{ encore_entry_link_tags('pagePreview') }}
    {% if page.theme.generatedCssPath %}
        <link rel="stylesheet" href="{{ path('app_theme_css', { id: page.theme.id }) }}">
    {% endif %}
    {% include '_page_fonts.html.twig' %}
</head>
<body class="min-h-screen p-6">
   
    <div class="mx-auto">
        <p class="mb-4 text-sm text-muted-foreground">
            <a href="{{ path('app_page_edit', { id: page.id }) }}" class="underline">← Retour à l'édition</a>
        </p>
        <div>
            <h1 class="text-3xl font-bold tracking-tight mb-6">{{ page.title }}</h1>
            <div class="prose prose-neutral dark:prose-invert max-w-none">
                {% if page.content is not empty %}
                    {% import 'page/_builder_api_base_url.html.twig' as builder_api %}
                    <div id="page-preview-root" data-json="{{ page.content|json_encode|e('html_attr') }}" data-api-cards-base-url="{{ builder_api.base_url()|e('html_attr') }}"></div>
                    {{ encore_entry_script_tags('pagePreview') }}
                {% else %}
                    <p class="text-muted-foreground">Aucun contenu.</p>
                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>
